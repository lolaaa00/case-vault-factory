CaseVaultFactory: every case is its own contract, not a row in a shared table. Every instance is both a factory and a self-contained milestone escrow. open_case() permissionlessly spawns a real, independent child via gl.deploy_contract, with its own GEN balance, own storage, own judged round -- addressable on the explorer as its own entity, not a TreeMap slot. The judged mechanism (fetch evidence, judge against criteria, dispute, deadline-gated reclaim) reuses this build's proven shape; the novel surface is the factory itself. Every sibling here keeps cases as rows in one shared contract, including an existing spawn method used only as a secondary convenience elsewhere -- here the factory IS the primitive. No owner, no admin: every reachable state has a permissionless escape. Verified live: funding a spawned child left the parent's case untouched, proving real storage isolation; a judged round returned UNSATISFIED in 29.8s; a child spawned its own grandchild. 51/51 direct tests pass.
